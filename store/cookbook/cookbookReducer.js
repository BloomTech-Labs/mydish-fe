const initState = {
    cookbookRecipes: [],
    isLoading: false,
    error: null,
};

export const cookbookReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
