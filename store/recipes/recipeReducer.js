const initState = {
    recipeList: [],
    isLoading: false,
    error: null
}

export const recipeReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}