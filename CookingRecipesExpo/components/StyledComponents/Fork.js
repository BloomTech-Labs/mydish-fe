import styled from 'styled-components';

const Fork = styled.View`
        flexDirection: row;
        justifyContent: flex-end;
        position: absolute;
        left : 135;
        top: 205;
        zIndex : 1;
`;

export default Fork;
